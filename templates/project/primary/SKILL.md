# SKILL: primary

- **Role:** primary source-of-truth
- **Mutability:** MUTABLE — authored and corrected over time.
- **Contains:** this project's most important material — the canonical, current
  version it is built around. Organize by type as needed: `docs/`, `media/`,
  `data/`, `code/` (use only the subset your project needs).
- **Purpose / how to use:** the authoritative basis for everything derived. Agents
  read these and, when explicitly asked, edit them; `output/` is produced **from**
  here. Back up before large edits.
- **Provenance:** refined by the author from `context/` (read-only research);
  feeds `output/`.
