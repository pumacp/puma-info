# Video <id> — script (scenes)

Scene script. Rules for this video live in `rules.md`; general rules in
`../../docs/video-rules.md`. One numbered entry per scene; each maps to a
section of `specs/<id>.json`.

1. **<Scene name>** — <one-line description of the scene>.
2. **<Scene name>** — <...>.
